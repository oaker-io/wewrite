# Credentials(永不入 git)
config.yaml
secrets/*
!secrets/README.md
.env
.env.*

# User data (generated at runtime, not tracked)
style.yaml
history.yaml
playbook.md
corpus/
lessons/
# 个人身份档案(从 xwrite vendor · 个人数据 · 不入 git)
identity/

# Optimized writing parameters (user runs loop to generate their own)
writing-config.yaml
optimization-results.tsv

# Generated output
output/
!output/.gitkeep
# Sanitize 临时副本(publish.py 写入 · 完成后自动清理 · 双保险)
*._publish.md

# Exemplar library (user-specific content)
references/exemplars/*.md
references/exemplars/index.yaml

# Legacy client directories
clients/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/*
!dist/openclaw/
build/

# Internal dev docs
docs/
# CLAUDE.md intentionally kept in git · it's the onboarding doc for new sessions

# IDE
.vscode/
.idea/

# Routine + Discord bot runtime artifacts
routine/logs/
discord-bot/logs/
venv/
